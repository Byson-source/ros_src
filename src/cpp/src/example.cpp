#include "example.h"
#include <string>


overloaded_window::overloaded_window(){}
overloaded_window::open_database(int value,string database_path){
if(value==1){
    openDatabase(database_path)
}

overloaded_window::close_database

overloaded_window::close_database(int value){


}